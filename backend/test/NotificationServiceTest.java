import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import launch.ApplicationConfig;

import static org.junit.jupiter.api.Assertions.*;

/**
 * Tests for NotificationService.
 */
public class NotificationServiceTest {
/*
    private NotificationService service;

    @BeforeEach
    void beforeEach() {
        service = ApplicationConfig.getConfiguration().getNotificationService();
    }

    @Test
    void serviceInitialized() {
        assertNotNull(service);
    }

    @Test
    void nextRefreshSignalReturnsFiniteDouble() {
        double signal = service.nextRefreshSignal();
        assertTrue(Double.isFinite(signal));
    }
    */
}
